spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/finflow_dev
    username: finflow_dev
    password: Rahul@#1345
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    baseline-on-migrate: true

jwt:
  secret: finflow-dev-secret-key-minimum-32-characters-long
  expiration-ms: 3600000

app:
  cors:
    allowed-origins: http://localhost:3000,http://localhost:5173

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
